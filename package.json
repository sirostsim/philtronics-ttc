{
    "name": "philtronics-timecomplete-root",
    "version": "1.0.0",
    "description": "Root package - delegates to server/",
    "scripts": {
          "start":   "cd server && node seed.js && node server.js",
          "install": "cd server && npm install"
    },
    "dependencies": {
          "@aws-sdk/client-s3": "^3.600.0",
          "@aws-sdk/lib-storage": "^3.600.0"
    },
    "engines": {
          "node": ">=20.0.0"
    }
}
